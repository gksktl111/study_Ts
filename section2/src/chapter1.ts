// ** number
// ** 타입주석 타입 어노테이션이라고 부름
let num: number = 123;

// ** string
let str: string = "Hello World";

// ** boolean
let bool: boolean = true;

// ** strictNullChecks
// ** 컴파일러가 null과 undefined를 타입 검사에 포함시키는 옵션
// ** 기본적으로 null과 undefined는 모든 타입에 할당 가능하지만
// ** strictNullChecks가 켜져있으면 타입 검사에 포함됨

// let num2: number = null;
